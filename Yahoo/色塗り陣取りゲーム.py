# 入力
n,m,k = map(int,input().split())
# 初期の色配置
color = []
color.append(list(map(int,input().split())))
# m本の道の情報
load = []
for i in range(m):
    load.append(list(map(int,input().split())))

# 各時間の色配置
for i in range(k):
    # 各地点の色を決める

# 出力
